spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_resume_assistant
    username: resume_admin
    password: Resume@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080